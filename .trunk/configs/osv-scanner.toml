# OSV Scanner configuration for walking-tracker
# Last updated: 2026-08-20

# image-size <= 2.0.2 vulnerabilities (GHSA-w3rx-r6r6-pgpr, GHSA-5p2g-fcmc-qvqq)
# No patch available as of 2026-08-20. Transitive dependency of metro (dev-only).
# Re-evaluate when a fix is released.

[[IgnoredVulns]]
id = "GHSA-w3rx-r6r6-pgpr"
reason = "image-size <= 2.0.2 Denial of Service; no patch available as of 2026-08-20. Transitive dependency of metro (dev-only), not in app bundle. Re-evaluate when a fix is released."

[[IgnoredVulns]]
id = "GHSA-5p2g-fcmc-qvqq"
reason = "image-size <= 2.0.2 Denial of Service; no patch available as of 2026-08-20. Transitive dependency of metro (dev-only), not in app bundle. Re-evaluate when a fix is released."
